export type Service = {
  title: string;
  description: string;
  features: string[];
  badge?: string;
};

export type Faq = {
  question: string;
  answer: string;
};

export type SelectOption = {
  value: string;
  label: string;
};
